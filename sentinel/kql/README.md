# KQL Detection Rules

These queries are templates. Table names and fields can differ by connector and configuration. Validate every query in your own Log Analytics workspace before enabling analytics rules.

For each rule, document:
- Query frequency and lookback
- Threshold
- Severity
- Entity mapping
- False-positive conditions
- Triage and closure steps
