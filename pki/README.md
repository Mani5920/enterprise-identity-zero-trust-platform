# PKI Lab

The scripts create certificate material only under `pki/generated/`, which is excluded from version control.

## Safety
- Run only on a system you own.
- Do not use lab certificates for production.
- Do not commit generated keys or certificates.
- Protect the root key password.
